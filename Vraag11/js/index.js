let dieren = ["varken", "koe", "paard", "hond", "kat"];
console.log (`De ingevoerde dieren zijn ${dieren}`);

console.log (`Het derde dier uit deze reeks is ${dieren[2]}`);

for (let i=0;i< dieren.length; i++){
    console.log (dieren[i]);
}